import SwiftUI

extension TCA {
    struct SavedPokemonView: View {
        var body: some View {
            VStack {
                Spacer()
                Text("Here will have the list of saved pokemons using TCA")
                Spacer()
            }
        }
    }

    #Preview {
        SavedPokemonView()
    }
}
